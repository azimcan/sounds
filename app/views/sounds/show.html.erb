<% title "Sound" %>
<div class="row m-2 p-2">
  <div class="col-12 shadow-lg p-3 mb-3 bg-white rounded">
    <h2 class="col-12"><%= @sound.name %></h2>
    <h5 class="col-12"><%= @sound.tag %></h5>
    <div class="col-12">
    	<iframe width="100%" height="166" scrolling="no" frameborder="no" allow="autoplay" src="<%= @sound.location %>"></iframe>
	    <div class="btn-group d-flex">
        <a class="btn btn-secondary" href="">Sepete Ekle</a>
        <%= link_to 'Duzenle', edit_sound_path(@sound), class: "btn btn-secondary" %>
        <%= link_to 'Geri', :back, class: "btn btn-secondary" %>
      </div>
    </div>
  </div>
</div>

<div class="m-2 p-2">
  <div class="col-12 shadow-lg p-3 mb-3 bg-white rounded">
    <h2 class="text-center">Yorumlar</h2>
    <% @sound.comments.each do |comment| %>
      <p>
        <%= link_to comment.user.name, profile_path(comment.user), class: "badge badge-info" %> : <%= comment.comment %>
      </p>
    <% end %>
    <%= "Yorum yok" if @sound.comments.blank?%>
    <hr>
    <%= render 'comment/form' %>
  </div>
</div>
